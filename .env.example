# Neon pooled connection string. Use the *pooled* endpoint (-pooler) for the app;
# drizzle-kit migrate works against either.
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.us-east-2.aws.neon.tech/invibot?sslmode=require"

# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET="replace-me"
BETTER_AUTH_URL="http://localhost:3000"

# WhatsApp Cloud API. Phone Number ID and WABA ID are on the app's API Setup
# page; the app secret is under Settings > Basic. The verify token is ours to
# invent — generate with `openssl rand -hex 24` and paste the same value into
# Meta's webhook configuration.
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_WABA_ID=""
WHATSAPP_BUSINESS_ID=""
WHATSAPP_ACCESS_TOKEN=""
WHATSAPP_APP_SECRET=""
WHATSAPP_WEBHOOK_VERIFY_TOKEN=""
